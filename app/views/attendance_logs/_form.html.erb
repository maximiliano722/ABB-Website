<%= form_with(model: attendance_log) do |form| %>
  <% if attendance_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attendance_log.errors.count, "error") %> prohibited this attendance_log from being saved:</h2>

      <ul>
        <% attendance_log.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :event_id %>
    <%= form.number_field :event_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
